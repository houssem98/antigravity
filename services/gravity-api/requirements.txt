# Gravity Search Backend Dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
orjson>=3.10.0
structlog>=24.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database clients
sqlalchemy[asyncio]>=2.0.0
psycopg[binary]>=3.0.0
redis[asyncio]>=5.0.0
elasticsearch[async]>=8.14,<9
qdrant-client>=1.9.0
neo4j>=5.20.0

# LLM clients
anthropic>=0.34.0
openai>=1.40.0
google-genai>=0.8.0
cohere>=5.0.0
# voyageai>=0.3.0  # Fails to build on Python 3.14
hermes-agent @ git+https://github.com/NousResearch/hermes-agent.git

# ML / Embeddings (optional — gracefully degrade if not available)
numpy>=1.26.0
transformers>=4.40.0
sentence-transformers>=3.0.0
tiktoken>=0.7.0  # token counting for chunker
aiofiles>=23.2.0  # async JSONL fallback for audit log
pyotp>=2.9.0  # TOTP MFA (RFC 6238)
passlib>=1.7.4  # password hashing (pbkdf2_sha256, no native deps)
zxcvbn>=4.4.28  # password strength scoring (Dropbox)
python-jose[cryptography]>=3.3.0  # JWT signing
email-validator>=2.0.0  # pydantic EmailStr validation
stripe>=15.0.0  # billing (A6) — kept for future non-TN deployments
# Paddle + Coinbase use raw httpx (no SDK needed)
qrcode[pil]>=7.4.0  # crypto wallet QR codes + MFA enrollment QR
pypng>=0.20220715  # pure-Python PNG backend for qrcode (MFA QR)
sentry-sdk[fastapi]>=2.0.0   # error tracking (plan §5.2)
opentelemetry-api>=1.25.0    # distributed tracing (plan §5.2 / §6.13)
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-httpx>=0.46b0
opentelemetry-exporter-otlp>=1.25.0

# PageIndex — VectifyAI tree-based retrieval (channel 6)
# pip install litellm pymupdf PyPDF2
litellm>=1.83.0
pymupdf>=1.24.0
PyPDF2>=3.0.1

# TurboQuant — compressed ANN index (channel 7; requires torch)
# pip install pyturboquant torch
# pyturboquant  # install from: github.com/jorgebmann/pyturboquant

# Kafka (event-driven ingestion pipeline)
aiokafka>=0.11.0

# HTTP
httpx>=0.27.0

# Document parsing (Table Reasoning paper enhancements)
pdfplumber>=0.11.0
pdfminer.six>=20221105
pypdf>=4.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Observability
langfuse>=2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
respx>=0.22.0

# LLM Evaluation (deepeval RAG triad)
deepeval>=2.0.0

# Memory system (local-first, semantic search)
mempalace>=3.3.5

# Time-series forecasting (Kronos foundation model)
einops>=0.8.0
safetensors>=0.4.0
pandas>=2.2.0  # required by app/api/routes/forecast.py (Kronos forecast endpoint)
# torch + huggingface_hub already pulled by transformers
